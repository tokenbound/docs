import { Callout } from "nextra-theme-docs";
import { Tab, Tabs } from 'nextra-theme-docs'

# getAccount

Fetches the ERC-6551 account for an NFT. Uses the Tokenbound wallet implementation by default.

<Tabs items={["viem", "Ethers.js"]}>
  <Tab>
    ```javascript
    import { getAccount } from "@tokenbound/sdk";

    const accountAddress = await getAccount(
      "0xe7134a029cd2fd55f678d6809e64d0b6a0caddcb", // ERC-712 contract address
      "9", // ERC-721 token ID
      publicClient // viem public client
    );
    ```
  </Tab>
  <Tab>
    ```javascript
    import { getAccount } from "@tokenbound/sdk-ethers";

    const accountAddress = await getAccount(
      "0xe7134a029cd2fd55f678d6809e64d0b6a0caddcb", // ERC-712 contract address
      "9", // ERC-721 token ID
      provider // ethers provider
    );
    ```
  </Tab>
</Tabs>

---

<Callout emoji="✍🏽">
  **Tokenbound** is suite of open source tools for building with [ERC-6551](). These docs are still a work in progress. In the meantime, feel free to join the [working group]() to learn more.
</Callout>
